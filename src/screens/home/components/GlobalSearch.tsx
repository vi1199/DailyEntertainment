import {VInput, VView} from '@src/ui';
import {StyleSheet} from 'react-native';

export function GlobalSearch() {
  return (
    <VView>
      <VInput />
    </VView>
  );
}
const styles = StyleSheet.create({
  container: {},
});
